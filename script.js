const botoeCurtir = document. querySelectorAll(".curtir");
botoeCurtir.fortach (function(botaoCurtir){
    let curtir = false;
    botaoCurtir.addEventListener("click",curtir);
function curtir(){
        const contador = botaoCurtir.querySelector("span");
        if(curtir === false)
    }
}